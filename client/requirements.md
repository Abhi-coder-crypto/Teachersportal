## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | For beautiful iconography
clsx | For conditional class merging
tailwind-merge | For merging tailwind classes utility

## Notes
Integration with Replit Auth or Local Auth via /api/auth endpoints as defined in routes.ts.
The application requires a clean, academic aesthetic with a blue/white theme.
Protected routes handling for Dashboard access.
Role-based access control (Admin vs Teacher) for resource management.
